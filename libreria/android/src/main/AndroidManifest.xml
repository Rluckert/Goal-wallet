<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Normal permission, granted at install time (no runtime prompt) — used for the
         haptic feedback triggered by parseDepositAmount on a valid deposit. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
